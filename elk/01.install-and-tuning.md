


## 1.Tuning

```console
cat << EOF > /etc/sysctl.d/elk_tunning.conf
vm.max_map_count = 262144
EOF
sysctl -p
sysctl --system
```

```
adduser tuanda
cat << EOF >> /etc/security/limits.conf 
tuanda  -  nofile  65535
tuanda  -  nproc  65535
tuanda  -  memlock unlimited
EOF
```

```console
# Disable swap
sudo swapoff –a
# Đặt vm.swappiness=1 trong sysctl để chặn memory swap
vim /etc/sysctl.conf
vm.swappiness=1 
```

```console
#Setting HeapSize = 50% memory of server and Xmx = Xms
vim elasticsearch.yml
Xmx=2G
Xms=2G
```

## 2. Install
### Bước 1: Cài đặt từ tar.gz
```console
cd /home/tuanda
su - tuanda #(nhớ giải nén, cài bằng user thường)
tar -xvzf elasticsearch-8.15.0-linux-x86_64.tar.gz
Ta tạo thư mục shortcut để tiện maintain về sau, nâng cấp version
ln -s /home/tuanda/elasticsearch-8.15.0 /home/tuanda/elasticsearch
mkdir /home/tuanda/elasticsearch/data
mkdir /home/tuanda/elasticsearch/logs
```

Mô hình 3 Master, 3 data node
```
192.168.88.12 Master+Data+ingest  
192.168.88.13 Master+Data+ingest    
192.168.88.14 Master+Data+ingest   
```

```
hostnamectl set-hostname node-1
hostnamectl set-hostname node-2
hostnamectl set-hostname node-3
```
```
cat << EOF >> /etc/hosts
192.168.88.12 node-1
192.168.88.13 node-2
192.168.88.14 node-3
EOF
```
```
sed -i -e 's/## -Xms4g/-Xms2g/g' /home/tuanda/elasticsearch/config/jvm.options
sed -i -e 's/## -Xmx4g/-Xmx2g/g' /home/tuanda/elasticsearch/config/jvm.options
```


### Bước 2: Set Security cho ES (Đặt mật khẩu + SSL)
```
https://www.elastic.co/guide/en/elasticsearch/reference/current/certutil.html 
https://opster.com/guides/elasticsearch/how-tos/update-elasticsearch-security-certificates 
cat << EOF > /tmp/instance.yml
instances:
  - name: 'node-1'
    ip: [ '192.168.88.12' ]
  - name: 'node-2'
    ip: [ '192.168.88.13' ]
  - name: 'node-3'
    ip: [ '192.168.88.14' ]
EOF
```
```
mkdir /home/tuanda/elasticsearch/config/cert
/home/tuanda/elasticsearch/bin/elasticsearch-certutil ca --pem --days 99999 --out /home/tuanda/elasticsearch/config/cert/elastic-stack-ca.zip
cd /home/tuanda/elasticsearch/config/cert/ ; unzip elastic-stack-ca.zip
/home/tuanda/elasticsearch/bin/elasticsearch-certutil cert --pem --ca-cert /home/tuanda/elasticsearch/config/cert/ca/ca.crt --ca-key /home/tuanda/elasticsearch/config/cert/ca/ca.key --in /tmp/instance.yml --days 99999 --out /home/tuanda/elasticsearch/config/cert/cert.zip
cd /home/tuanda/elasticsearch/config/cert/; unzip cert.zip
Trên từng server, tạo thư mục cert và copy 3 file vào thư mục này bao gồm:
-	ca/ca.crt
-	node-xxx/node-xxx.crt
-	node-xxx/node-xxx.key
Các node 2,3 tương tự, chỉ cần copy đúng node-x/node-x.crt là được.
```
Do khai báo ở systemd là -Des.path.conf=/home/tuanda/elasticsearch/config, nên thư mục cert phải nằm trong thư mục config (https://discuss.elastic.co/t/trying-to-set-up-tls-on-elastic-cluster/143323/6 )
```
su – tuanda
scp -r /home/tuanda/elasticsearch/config/cert 192.168.88.13:/home/tuanda/elasticsearch/config/
scp -r /home/tuanda/elasticsearch/config/cert 192.168.88.14:/home/tuanda/elasticsearch/config/
```

Cấu hình node-1
```
cat << EOF > /home/tuanda/elasticsearch/config/elasticsearch.yml
cluster.name: elastic-cluster-tuanda # đặt tên cho cụm
# tên của node, thường đặt là chính hostname
node.name: node-1
node.roles: [master, data, ingest]
path.data: /home/tuanda/elasticsearch/data
path.logs: /home/tuanda/elasticsearch/logs
# network.host  là IP node đó đang setting
network.host: 192.168.88.12
# khai báo toàn bộ các IP trong cụm, có thể bỏ qua chính host đang khai báo.
discovery.seed_hosts: ["node-1", "node-2","node-3"]
# Chỉ khai báo những Master-node. khi khởi chạy ES, code sẽ báo join vào cụm bằng cách thông báo 3 node này
cluster.initial_master_nodes: ["node-1", "node-2","node-3"]

#SSL cho Cluster
xpack.security.enabled: true
xpack.security.transport.ssl.enabled: true
xpack.security.transport.ssl.key: /home/tuanda/elasticsearch/config/cert/node-1/node-1.key
xpack.security.transport.ssl.certificate: /home/tuanda/elasticsearch/config/cert/node-1/node-1.crt
xpack.security.transport.ssl.certificate_authorities: /home/tuanda/elasticsearch/config/cert/ca/ca.crt

# Để bật https://es:9200 SSL lên. Ta dùng option dưới đây
#xpack.security.http.ssl.enabled: true
#xpack.security.http.ssl.key: /home/tuanda/elasticsearch/config/cert/node-1/node-1.key
#xpack.security.http.ssl.certificate: /home/tuanda/elasticsearch/config/cert/node-1/node-1.crt
#xpack.security.http.ssl.certificate_authorities: /home/tuanda/elasticsearch/config/cert/ca/ca.crt
EOF
```
Cấu hình node-2
```
cat << EOF > /home/tuanda/elasticsearch/config/elasticsearch.yml
cluster.name: elastic-cluster-tuanda
node.name: node-2
node.roles: [master, data, ingest]
path.data: /home/tuanda/elasticsearch/data
path.logs: /home/tuanda/elasticsearch/logs
network.host: 192.168.88.13
discovery.seed_hosts: ["node-1", "node-2","node-3"]
cluster.initial_master_nodes: ["node-1", "node-2","node-3"]

xpack.security.enabled: true
xpack.security.transport.ssl.enabled: true
xpack.security.transport.ssl.key: /home/tuanda/elasticsearch/config/cert/node-2/node-2.key
xpack.security.transport.ssl.certificate: /home/tuanda/elasticsearch/config/cert/node-2/node-2.crt
xpack.security.transport.ssl.certificate_authorities: /home/tuanda/elasticsearch/config/cert/ca/ca.crt
EOF
```
Cấu hình node-3
```
cat << EOF > /home/tuanda/elasticsearch/config/elasticsearch.yml
cluster.name: elastic-cluster-tuanda
node.name: node-3
node.roles: [master, data, ingest]
path.data: /home/tuanda/elasticsearch/data
path.logs: /home/tuanda/elasticsearch/logs
network.host: 192.168.88.14
discovery.seed_hosts: ["node-1", "node-2","node-3"]
cluster.initial_master_nodes: ["node-1", "node-2","node-3"]

xpack.security.enabled: true
xpack.security.transport.ssl.enabled: true
xpack.security.transport.ssl.key: /home/tuanda/elasticsearch/config/cert/node-3/node-3.key
xpack.security.transport.ssl.certificate: /home/tuanda/elasticsearch/config/cert/node-3/node-3.crt
xpack.security.transport.ssl.certificate_authorities: /home/tuanda/elasticsearch/config/cert/ca/ca.crt
EOF
```

Tạo systemd cho elasticsearch trên cả 3 node
https://github.com/elastic/elasticsearch/blob/main/distribution/packages/src/common/systemd/elasticsearch.service

```
touch /home/tuanda/elasticsearch/elasticsearch.pid  #(chú ý cần tạo file này , thì mới bật lên đc)

cat << 'EOF' > /etc/systemd/system/elasticsearch.service
[Unit]
Description=Elasticsearch
Documentation=https://www.elastic.co
Wants=network-online.target
After=network-online.target

[Service]
Type=simple
NotifyAccess=all
RuntimeDirectory=elasticsearch
PrivateTmp=true
Environment=ES_HOME=/home/tuanda/elasticsearch
Environment=ES_PATH_CONF=/home/tuanda/elasticsearch/config
Environment=PID_DIR=/home/tuanda/elasticsearch
Environment=ES_SD_NOTIFY=true

WorkingDirectory=/home/tuanda/elasticsearch

User=tuanda
Group=tuanda

ExecStart=/home/tuanda/elasticsearch/bin/elasticsearch -p ${PID_DIR}/elasticsearch.pid --quiet


StandardOutput=journal
StandardError=inherit

# Specifies the maximum file descriptor number that can be opened by this process
LimitNOFILE=65535

# Specifies the maximum number of processes
LimitNPROC=4096

# Specifies the maximum size of virtual memory
LimitAS=infinity

# Specifies the maximum file size
LimitFSIZE=infinity

# Disable timeout logic and wait until process is stopped
TimeoutStopSec=0

# SIGTERM signal is used to stop the Java process
KillSignal=SIGTERM

# Send the signal only to the JVM rather than its control group
KillMode=process

# Java process is never killed
SendSIGKILL=no

# When a JVM receives a SIGTERM signal it exits with code 143
SuccessExitStatus=143

# Allow a slow startup before the systemd notifier module kicks in to extend the timeout
TimeoutStartSec=900

[Install]
WantedBy=multi-user.target
# Built for @project.name@-@project.version@ (@project.name@)
EOF
```

Test thử trên 3 node xem đã oke chưa, trước khi bật systemd
```
cd /home/tuanda/elasticsearch/bin
./elasticsearch

sudo systemctl daemon-reload
sudo systemctl restart elasticsearch
ps aux | grep elasticsearch
tail -f /home/tuanda/elasticsearch/logs/*_server.json
```

### Bước 3: Setup Passswod
```
/home/tuanda/elasticsearch/bin/elasticsearch-setup-passwords interactive
```

Kiểm tra sau khi cài đặt
```
# curl 192.168.88.12:9200 -u elastic:123456
# curl -XGET 192.168.88.12:9200/_cluster/health?pretty -u elastic:123456
# curl -vk -XGET http://192.168.88.14:9200/_cluster/health?pretty -u elastic:123456
{
  "cluster_name" : "elastic-cluster-tuanda",
  "status" : "green",
  "timed_out" : false,
  "number_of_nodes" : 3,
  "number_of_data_nodes" : 3,
  "active_primary_shards" : 1,
  "active_shards" : 2,
  "relocating_shards" : 0,
  "initializing_shards" : 0,
  "unassigned_shards" : 0,
  "delayed_unassigned_shards" : 0,
  "number_of_pending_tasks" : 0,
  "number_of_in_flight_fetch" : 0,
  "task_max_waiting_in_queue_millis" : 0,
  "active_shards_percent_as_number" : 100.0
}
```

 (Trường hợp thêm node, thì cần tạo lại toàn bộ ssl và làm lại.)


### Nginx
```
cat << 'EOF' > /etc/nginx/conf.d/es.conf
server {
    listen 80;

    server_name elasticsearch.tuan.name.vn;

    location / {
        proxy_pass http://192.168.88.12:9200/;
        proxy_http_version 1.1;
        #proxy_set_header Authorization 'Basic ZWxhc3RpYzoxMjM0NTY=';
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;        
    }
}
EOF
```

